select Employee.Id, FullName.Last_Name, FullName.First_Name, FullName.Patronymic from Department inner join Employee on Department.Employee = Employee.Id inner join[Department Name] on Department.[Department Name] = [Department Name].Id inner join FullName on FullName.Id = Employee.[Full Name] where[Department Name] = 1